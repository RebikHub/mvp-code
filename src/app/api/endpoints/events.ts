/*
@app.route('/events/invoke/check_days_lambda', methods=['GET'], cors=True)
def invoke_check_days_lambda():
    return delegate_schedule.forced_start_check_days_schedule_lambda(app.current_request)

@app.route('/events/invoke/check_date_lambda', methods=['GET'], cors=True)
def invoke_check_date_lambda():
    return delegate_schedule.forced_start_check_date_schedule_lambda(app.current_request)
*/
